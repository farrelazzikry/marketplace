services:
  - type: web
    name: marketplace
    env: php
    buildCommand: |
      composer install --no-dev
      npm install
      npm run build
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT